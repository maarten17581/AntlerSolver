#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <fstream>
#include <string>
#include <cassert>

#include "Graph.h"

bool is_acyclic(Graph& g) {

}

Graph subgraph(Graph& g, std::vector<uint32_t> nodes) {

}

std::vector<Graph> connected_components(Graph& g) {

}

std::vector<uint32_t> fvs_naive(Graph& g) {
    std::vector<uint32_t> nodes;
    
}